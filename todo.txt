
1) * implement all attribute bindings
    # * прописать в шаблоне mx-autocomplete данные, получаемые из атрибутов mx-picker
        mx-picker-new template
        # combine /w mx-autocomplete bindings

        - compare controllers (mx-new mx-picker mx-autocomplete)


2) сделать чтобы работало без нового кода со старым baseController
    # максимально использовать имеющийся код из baseController ( найти аналоги того что используется в link mdAutocomplete )
    # валидация при focus / blur ( использовать из baseController )

3) убрать все из mdAutocomplete в mxPicker \ пропатчить внутренний mdAutocomplete с помощью доп. директивы-атрибута в шаблоне mxPicker
    # уменьшить кол-во dom-манипуляций

4) рефакторить часть функционала baseController в отдельный service
    # хранить данные, уникальные для каждой директивы в controller instance

- сделать функцию фильтрации по любому фрагменту внутри слова

- add unit tests

* implement vm.notFoundDescriptor

--------------------------------------------------------------------------------

- delete stub function from mxPickerCtrl

# vm.selectedValue                   - alias to vm.model
# BasePickerProperties.selectedItems - в singlePicker лишнее

# singlePicker
    singlePickerController + commonPickerProperties

# autocompletePicker
    autocompleteController + singlePickerController + BasePickerProperties

{
    searchText   : 'Default Value',
    selectedItem : 'Default object'
}

--------------------------------------------------------------------------------

http://www.bennadel.com/blog/2926-overriding-directive-definitions-in-angularjs.htm
http://www.webdeveasy.com/angularjs-data-model/
